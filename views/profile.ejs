<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Dashboard - Edsam Agents</title>
  <link href="https://fonts.googleapis.com/css2?family=Segoe+UI:wght@400;600&display=swap" rel="stylesheet">
  <style>
    /* Sidebar */
    .sidebar {
      width: 250px;
      background: #fff;
      border-right: 1px solid #eee;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding: 1rem;
      position: fixed;
      top: 0;
      bottom: 0;
      overflow-y: auto;
    }

    .sidebar .logo img {
      width: 30%;
      margin-bottom: 2rem;
    }

    .nav {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .nav a {
      padding: 0.7rem 1rem;
      border-radius: 0.5rem;
      font-size: 0.95rem;
      transition: 0.3s;
    }

    .nav a:hover,
    .nav a.active {
      background: #2563eb;
      color: #fff;
    }

    .user-box {
      background: #f1f5f9;
      padding: 1rem;
      border-radius: 0.75rem;
      text-align: center;
    }



    .user-box img {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      margin-bottom: 0.5rem;
    }
  </style>
  <style>
    /* Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: #f9fafb;
      color: #333;
      display: flex;
      height: 100vh;
      overflow: hidden;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    /* Sidebar */
    .sidebar {
      width: 250px;
      background: #fff;
      border-right: 1px solid #eee;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding: 1.2rem;
      position: fixed;
      top: 0;
      bottom: 0;
      left: 0;
      z-index: 200;
      transition: left 0.3s ease;
    }

    .sidebar.mobile-hidden {
      left: -260px;
    }

    .sidebar .logo img {
      width: 40%;
      margin-bottom: 2rem;
    }

    .nav {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .nav a {
      padding: .75rem 1rem;
      border-radius: .5rem;
      font-size: .95rem;
      font-weight: 500;
      color: #444;
      transition: .3s;
    }

    .nav a:hover,
    .nav a.active {
      background: #2563eb;
      color: #fff;
    }

    .user-box {
      background: #f1f5f9;
      padding: 1rem;
      border-radius: .75rem;
      text-align: center;
    }

    .user-box img {
      width: 55px;
      height: 55px;
      border-radius: 50%;
      margin-bottom: .5rem;
    }

    /* Topbar */
    .topbar {
      position: fixed;
      top: 0;
      left: 250px;
      right: 0;
      height: 65px;
      background: #2563eb;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 24px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.15);
      z-index: 150;
    }

    .hamburger {
      display: none;
      flex-direction: column;
      gap: 5px;
      cursor: pointer;
    }

    .hamburger span {
      width: 22px;
      height: 3px;
      background: #fff;
      border-radius: 3px;
    }

    /* Main */
    .main {
      margin-left: 250px;
      flex: 1;
      overflow-y: auto;
      padding: 90px 2rem 2rem;
      animation: fadeIn .8s ease;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Dashboard Widgets */
    .widgets {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.2rem;
      margin-bottom: 2rem;
    }

    .widget {
      background: #fff;
      border-radius: .8rem;
      box-shadow: 0 2px 6px rgba(0, 0, 0, .1);
      padding: 1.2rem;
      display: flex;
      flex-direction: column;
      gap: .5rem;
      transition: .3s;
    }

    .widget:hover {
      transform: translateY(-3px);
      box-shadow: 0 5px 12px rgba(0, 0, 0, .12);
    }

    .widget h3 {
      font-size: 1.1rem;
      color: #2563eb;
    }

    .widget p {
      font-size: 1.5rem;
      font-weight: 600;
      color: #111;
    }

    /* Grid Listings */
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
      gap: 1.5rem;
    }

    .card {
      background: #fff;
      border-radius: 1rem;
      box-shadow: 0 3px 8px rgba(0, 0, 0, .08);
      overflow: hidden;
      transition: all .3s ease;
      display: flex;
      flex-direction: column;
    }

    .card:hover {
      transform: translateY(-6px);
      box-shadow: 0 6px 16px rgba(0, 0, 0, .15);
    }

    .card img {
      width: 100%;
      height: 180px;
      object-fit: cover;
    }

    .card-content {
      padding: 1rem;
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .card h3 {
      font-size: 1.05rem;
      margin-bottom: .4rem;
      font-weight: 600;
      color: #111;
    }

    .price {
      font-weight: bold;
      color: #2563eb;
      margin-bottom: .4rem;
    }

    .tags {
      font-size: .85rem;
      color: #555;
      text-transform: capitalize;
    }

    .actions {
      margin-top: 1rem;
      display: flex;
      gap: .5rem;
    }

    .actions button {
      flex: 1;
      padding: .5rem;
      border: none;
      border-radius: .5rem;
      cursor: pointer;
      font-size: .85rem;
      transition: .3s;
    }

    .actions .view {
      background: #2563eb;
      color: #fff;
    }

    .actions .edit {
      background: #f1f5f9;
      color: #333;
    }

    .actions .boost {
      background: #16a34a;
      color: #fff;
    }

    .actions button:hover {
      opacity: .9;
    }

    /* Loader */
    #loader {
      position: fixed;
      inset: 0;
      background: #f9fafb;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      z-index: 9999;
      transition: opacity .5s ease;
    }

    #loader img {
      width: 120px;
      margin-bottom: 15px;
      animation: pulseLogo 1.5s infinite;
    }

    .loader-motto {
      font-size: 1.2rem;
      font-weight: 500;
      color: #2563eb;
      margin-bottom: 20px;
    }

    .loader-bar {
      width: 220px;
      height: 8px;
      background: #e5e7eb;
      border-radius: 4px;
      overflow: hidden;
    }

    .loader-progress {
      width: 0%;
      height: 100%;
      background: #2563eb;
      animation: loadProgress 3s forwards;
    }

    @keyframes loadProgress {
      from {
        width: 0%
      }

      to {
        width: 100%
      }
    }

    @keyframes pulseLogo {

      0%,
      100% {
        transform: scale(1)
      }

      50% {
        transform: scale(1.05)
      }
    }

    /* Responsive */
    @media(max-width:768px) {
      .hamburger {
        display: flex;
      }

      .sidebar {
        left: -260px;
      }

      .sidebar.show {
        left: 0;
      }

      .topbar {
        left: 0;
        padding: 0 15px;
      }

      .main {
        margin-left: 0;
        padding: 90px 1rem 1rem;
      }

      .widgets {
        grid-template-columns: 1fr 1fr;
      }
    }

    /* Loader */
    #loader {
      position: fixed;
      inset: 0;
      background: #f9fafb;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      z-index: 9999;
      transition: opacity 0.5s ease;
    }

    #loader img {
      width: 120px;
      margin-bottom: 15px;
      animation: pulseLogo 1.5s infinite;
    }

    .loader-motto {
      font-size: 1.2rem;
      font-weight: 500;
      color: #2563eb;
      margin-bottom: 20px;
    }

    /* Notifications */
    .notifications-container {
      position: fixed;
      top: 20px;
      right: 20px;
      max-width: 340px;
      display: flex;
      flex-direction: column;
      gap: 15px;
      z-index: 9999;
    }

    .notification-card {
      position: relative;
      background: linear-gradient(135deg, #4f46e5, #3b82f6);
      color: #fff;
      padding: 15px 20px;
      border-radius: 15px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
      animation: slideIn 0.7s ease, float 3s ease-in-out infinite;
      font-size: 14px;
      line-height: 1.4;
    }

    .notification-header {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 15px;
      margin-bottom: 6px;
    }

    .notification-card a {
      color: #ffdd57;
      font-weight: bold;
    }

    .notification-card a:hover {
      text-decoration: underline;
    }

    .close-btn {
      position: absolute;
      top: 6px;
      right: 8px;
      background: none;
      border: none;
      color: #fff;
      font-size: 18px;
      font-weight: bold;
      cursor: pointer;
      transition: transform 0.2s ease;
    }

    .close-btn:hover {
      transform: scale(1.2);
      color: #ff4d4d;
    }

    .loader-bar {
      width: 220px;
      height: 8px;
      background: #e5e7eb;
      border-radius: 4px;
      overflow: hidden;
    }

    .loader-progress {
      width: 0%;
      height: 100%;
      background: #2563eb;
      animation: loadProgress 1s forwards;
      /* 1 second animation */
    }

    @keyframes loadProgress {
      from {
        width: 0%;
      }

      to {
        width: 100%;
      }
    }

    @keyframes pulseLogo {

      0%,
      100% {
        transform: scale(1);
      }

      50% {
        transform: scale(1.05);
      }
    }
  </style>
  <style>
    /* Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: #f9fafb;
      color: #333;
      display: flex;
      height: 100vh;
      overflow: hidden;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    /* Sidebar */
    .sidebar {
      width: 250px;
      background: #fff;
      border-right: 1px solid #eee;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding: 1.2rem;
      position: fixed;
      top: 0;
      bottom: 0;
      left: 0;
      z-index: 200;
      transition: left 0.3s ease;
    }

    .sidebar.mobile-hidden {
      left: -260px;
    }

    .sidebar .logo img {
      width: 30%;
      margin-bottom: 2rem;
    }

    .nav {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .nav a {
      padding: 0.75rem 1rem;
      border-radius: 0.5rem;
      font-size: 0.95rem;
      font-weight: 500;
      color: #444;
      transition: 0.3s;
    }

    .nav a:hover,
    .nav a.active {
      background: #2563eb;
      color: #fff;
    }

    .user-box {
      background: #f1f5f9;
      padding: 1rem;
      border-radius: 0.75rem;
      text-align: center;
    }

    .user-box img {
      width: 55px;
      height: 55px;
      border-radius: 50%;
      margin-bottom: 0.5rem;
    }

    /* Topbar */
    .topbar {
      position: fixed;
      top: 0;
      left: 250px;
      right: 0;
      height: 65px;
      background: #2563eb;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 24px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.15);
      z-index: 150;
    }

    .hamburger {
      display: none;
      flex-direction: column;
      gap: 5px;
      cursor: pointer;
    }

    .hamburger span {
      width: 22px;
      height: 3px;
      background: #fff;
      border-radius: 3px;
    }

    /* Filters */
    .filters {
      display: flex;
      gap: 0.8rem;
      align-items: center;
    }

    .filters select,
    .search-bar input {
      padding: 0.45rem 0.6rem;
      border-radius: 0.5rem;
      border: none;
      outline: none;
      font-size: 0.9rem;
    }

    .search-bar input {
      width: 200px;
    }

    /* Main Content */
    .main {
      margin-left: 250px;
      flex: 1;
      overflow-y: auto;
      padding: 90px 2rem 2rem;
      animation: fadeIn 0.8s ease;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Section Titles */
    .section-title {
      font-size: 1.6rem;
      margin: 2rem 0 1rem;
      font-weight: 700;
      color: #111;
    }

    .vip-title {
      color: #b45309;
      text-align: center;
      font-size: 1.9rem;
      margin: 2rem 0 1.5rem;
      text-shadow: 0 0 8px rgba(255, 215, 0, 0.6);
    }

    /* Grid */
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
      gap: 1.5rem;
    }

    /* Card */
    .card {
      background: #fff;
      border-radius: 1rem;
      box-shadow: 0 3px 8px rgba(0, 0, 0, 0.08);
      overflow: hidden;
      transition: all .3s ease;
      display: flex;
      flex-direction: column;
      position: relative;
    }

    .card:hover {
      transform: translateY(-6px);
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.15);
    }

    .card img {
      width: 100%;
      height: 180px;
      object-fit: cover;
    }

    .card-content {
      padding: 1rem;
    }

    .card h3 {
      font-size: 1.05rem;
      margin-bottom: 0.4rem;
      font-weight: 600;
      color: #111;
    }

    .price {
      font-weight: bold;
      color: #2563eb;
      margin-bottom: 0.4rem;
    }

    .details {
      font-size: 0.85rem;
      color: #555;
    }

    .badge {
      position: absolute;
      top: 12px;
      left: 12px;
      background: #2563eb;
      color: #fff;
      padding: 0.3rem 0.7rem;
      border-radius: 0.5rem;
      font-size: 0.75rem;
      font-weight: 600;
    }

    /* VIP Card */
    .vip-card {
      background: linear-gradient(135deg, #facc15, #f97316);
      color: #111;
      box-shadow: 0 8px 25px rgba(250, 204, 21, 0.5);
      animation: pulseGlow 3s infinite;
    }

    .vip-card h3 {
      color: #7c2d12;
    }

    .vip-card .price {
      color: #1e40af;
    }

    .vip-card .badge {
      background: #b45309;
      color: #fff7e6;
    }

    @keyframes pulseGlow {

      0%,
      100% {
        box-shadow: 0 8px 25px rgba(250, 204, 21, 0.4);
      }

      50% {
        box-shadow: 0 12px 35px rgba(250, 204, 21, 0.7);
      }
    }

    /* Notifications */
    .notifications-container {
      position: fixed;
      top: 20px;
      right: 20px;
      max-width: 340px;
      display: flex;
      flex-direction: column;
      gap: 15px;
      z-index: 9999;
    }

    .notification-card {
      position: relative;
      background: linear-gradient(135deg, #4f46e5, #3b82f6);
      color: #fff;
      padding: 15px 20px;
      border-radius: 15px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
      animation: slideIn 0.7s ease, float 3s ease-in-out infinite;
      font-size: 14px;
      line-height: 1.4;
    }

    .notification-header {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 15px;
      margin-bottom: 6px;
    }

    .notification-card a {
      color: #ffdd57;
      font-weight: bold;
    }

    .notification-card a:hover {
      text-decoration: underline;
    }

    .close-btn {
      position: absolute;
      top: 6px;
      right: 8px;
      background: none;
      border: none;
      color: #fff;
      font-size: 18px;
      font-weight: bold;
      cursor: pointer;
      transition: transform 0.2s ease;
    }

    .close-btn:hover {
      transform: scale(1.2);
      color: #ff4d4d;
    }

    @keyframes slideIn {
      from {
        transform: translateX(120%);
        opacity: 0;
      }

      to {
        transform: translateX(0);
        opacity: 1;
      }
    }

    @keyframes slideOut {
      from {
        opacity: 1;
        transform: translateX(0);
      }

      to {
        opacity: 0;
        transform: translateX(120%);
      }
    }

    @keyframes float {
      0% {
        transform: translateY(0px);
      }

      50% {
        transform: translateY(-6px);
      }

      100% {
        transform: translateY(0px);
      }
    }

    /* Loader */
    #loader {
      position: fixed;
      inset: 0;
      background: #f9fafb;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      z-index: 9999;
      transition: opacity 0.5s ease;
    }

    #loader img {
      width: 120px;
      margin-bottom: 15px;
      animation: pulseLogo 1.5s infinite;
    }

    .loader-motto {
      font-size: 1.2rem;
      font-weight: 500;
      color: #2563eb;
      margin-bottom: 20px;
    }

    .loader-bar {
      width: 220px;
      height: 8px;
      background: #e5e7eb;
      border-radius: 4px;
      overflow: hidden;
    }

    .loader-progress {
      width: 0%;
      height: 100%;
      background: #2563eb;
      animation: loadProgress 3s forwards;
    }

    @keyframes loadProgress {
      from {
        width: 0%
      }

      to {
        width: 100%
      }
    }

    @keyframes pulseLogo {

      0%,
      100% {
        transform: scale(1)
      }

      50% {
        transform: scale(1.05)
      }
    }

    /* Responsive */
    .desktop-only {
      display: flex;
    }

    /* Dark overlay for sidebar */
    #overlay {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.55);
      z-index: 180;
      opacity: 0;
      visibility: hidden;
      transition: opacity 0.3s ease;
    }

    #overlay.show {
      opacity: 1;
      visibility: visible;
    }


    .mobile-only {
      display: none;
    }

    @media(max-width:768px) {
      .hamburger {
        display: flex;
      }

      .sidebar {
        left: -260px;
      }

      .sidebar.show {
        left: 0;
      }

      .topbar {
        left: 0;
        padding: 0 15px;
      }

      .main {
        margin-left: 0;
        padding: 90px 1rem 1rem;
      }

      .filters {
        flex-wrap: wrap;
        gap: 0.5rem;
      }

      .search-bar input {
        width: 130px;
      }

      .desktop-only {
        display: none;
      }

      .mobile-only {
        display: inline-block;
      }
    }

    .warning {
      color: greenyellow;

    }
  </style>
</head>
<!-- Loader -->
<div id="loader">
  <img src="/NeonX Logo on Dark Backdrop.png" alt="NeonX Logo">
  <span class="warning">
    boost your ads to be the most visible
  </span><br>
  <h2 class="loader-motto">Loading Property Details...</h2>
  <div class="loader-bar">
    <div class="loader-progress"></div>
  </div>
</div>

<body>

  <!-- Sidebar -->
  <aside class="sidebar" id="sidebar">
    <div>
      <div class="logo"><img loading="lazy" src="/NeonX Logo on Dark Backdrop.png" alt="NeonX Logo"></div>
      <nav class="nav">
        <a href="/dashboard">Homepage</a>
        <a href="#" class="active">Dashboard</a>
        <a href="/sell">Add Listing</a>
        <a href="/inbox">Messages</a>
        <a href="/user">Profile</a>
        <a href="/freinds">My Friends</a>
        <a href="/saved">Saved Ads</a>
        <a href="/logout" onclick="return confirmLogout(event)">Logout</a>
      </nav>
    </div>
    <div class="user-box">
      <style>
        .profile-avatar {
          width: 56px;
          height: 56px;
          border-radius: 50%;
          display: flex;
          align-items: center;
          justify-content: center;
          font-size: 1.4rem;
          font-weight: 600;
          color: #fff;
          margin: 0 auto 0.5rem;
          user-select: none;
        }
      </style>
      <div class="user-box">
        <div class="profile-avatar" data-name="<%= user.username %>">
          <%= user.username.charAt(0).toUpperCase() %>
        </div>
        <strong>
          <%= user.username %>
        </strong>
        <small>Premium Seller</small>
      </div>
      <script>
        function getAvatarColor(name) {
          const colors = [
            "#1a73e8", // Google blue
            "#34a853", // Google green
            "#fbbc05", // Google yellow
            "#ea4335", // Google red
            "#9334e6", // purple
            "#f06292", // pink
            "#00acc1", // cyan
            "#5c6bc0"  // indigo
          ];

          let hash = 0;
          for (let i = 0; i < name.length; i++) {
            hash = name.charCodeAt(i) + ((hash << 5) - hash);
          }

          return colors[Math.abs(hash) % colors.length];
        }

        document.querySelectorAll(".profile-avatar").forEach(el => {
          const name = el.dataset.name || "U";
          el.style.backgroundColor = getAvatarColor(name);
        });
      </script>
  </aside>
  <div class="overlay" id="overlay">

  </div>
  <!-- Topbar -->
  <div class="topbar">
    <div class="hamburger" id="hamburger"><span></span><span></span><span></span></div>
    <h1>Dashboard</h1>
    <style>
      .boost-btn {
        display: inline-block;
        margin-top: 1rem;
        padding: 0.8rem 1.5rem;
        background: #f59e0b;
        color: #fff;
        font-weight: 600;
        border: none;
        border-radius: 0.6rem;
        cursor: pointer;
        transition: all 0.3s ease;
      }

      .boost-btn:hover {
        background: #d97706;
        transform: scale(1.05);
      }
    </style>
    <button type="submit" class="boost-btn"><a href="/sell">sell</a></button>
  </div>

  <!-- Main -->
  <main class="main">
    <!-- Widgets -->
    <div class="widgets">
      <div class="widget">
        <h3>Total Listings</h3>
        <p>
          <%= house.length + fashion.length + vehicle.length + furniture.length %>
        </p>
      </div>
      <a href="/sell">
        <div class="widget">
          <h3>Post Listing</h3>
          <p>
            click here
          </p>
        </div>
      </a>
      <a href="/seller/<%=user.email%>/<%=user._id%>/listings">
        <div class="widget">
          <h3>Check Your Page</h3>
          <p>
            click here
          </p>
        </div>
      </a>
      <a href="/feedback/<%=user._id%>">
        <div class="widget">
          <h3>Check Your Feedbacks</h3>
          <p>
            click here
          </p>
        </div>
      </a>
    </div>

    </div>

    <h2 style="margin-bottom:1rem;">My Listings</h2>
    <div id="messages" class="grid">
      <% house.forEach(list=> { %>
        <div class="card">


          <img loading="lazy" src="<%= list.bgimg %>" alt="Listing" />
          <div class="card-content">
            <h3>
              <%= list.title %>
            </h3>
  <%if(list.country=='ghana' ){%>
                            <div class="price">₵<%=Number(list.price).toLocaleString()%>
                            </div>
                            <%}%>
                              <%if(list.country=='nigeria' ){%>
                                <div class="price">₦<%=Number(list.price).toLocaleString()%>
                                </div>
                                <%}%>            </div>
            <div class="tags">
              <span class="tag">

                <style>
                  .tag {
                    text-transform: capitalize;
                  }
                </style>
                <%=list.country%>,<%= list.location %>, <%=list.specific%>
              </span>
            </div>
            <div class="actions">
              <a href="/property/<%=list.category%>/<%= list._id %>"><button class="view">View</button></a>
              <a class="boost" href="/boost/<%=list.category%>/<%=list._id%>"><button> boost visibility ⚡</button></a>
                                <a class="boost" href="/edit/<%=list.category%>/<%=list._id%>"><button>📝 Edit</button></a>

              <a href="/edit-listing/<%= list._id %>"><button class="edit"
                  onclick="return confirmDelete(event)">Delete</button></a>
            </div>
          </div>

        </div>
        <% }) %>
          <% services.forEach(list=> { %>
            <div class="card">


              <img loading="lazy" src="<%= list.bgimg %>" alt="Listing" />
              <div class="card-content">
                <h3>
                  <%= list.title %>
                </h3>
  <%if(list.country=='ghana' ){%>
                            <div class="price">₵<%=Number(list.price).toLocaleString()%>
                            </div>
                            <%}%>
                              <%if(list.country=='nigeria' ){%>
                                <div class="price">₦<%=Number(list.price).toLocaleString()%>
                                </div>
                                <%}%>                </div>
                <div class="tags">
                  <span class="tag">

                    <style>
                      .tag {
                        text-transform: capitalize;
                      }
                    </style>
                    <%= list.location %>, <%=list.specific%>
                  </span>
                </div>
                <div class="actions">
                  <a href="/property/<%=list.category%>/<%= list._id %>"><button class="view">View</button></a>
                  <a class="boost" href="/boost/<%=list.category%>/<%=list._id%>"><button> boost visibility
                      ⚡</button></a>
                                <a class="boost" href="/edit/<%=list.category%>/<%=list._id%>"><button>📝 Edit</button></a>

                  <a href="/edit-listing/<%= list._id %>"><button class="edit"
                      onclick="return confirmDelete(event)">Delete</button></a>
                </div>
              </div>

            </div>
            <% }) %>
              <% fashion.forEach(list=> { %>
                <div class="card">


                  <img loading="lazy" src="<%= list.bgimg %>" alt="Listing" />
                  <div class="card-content">
                    <h3>
                      <%= list.title %>
                    </h3>
  <%if(list.country=='ghana' ){%>
                            <div class="price">₵<%=Number(list.price).toLocaleString()%>
                            </div>
                            <%}%>
                              <%if(list.country=='nigeria' ){%>
                                <div class="price">₦<%=Number(list.price).toLocaleString()%>
                                </div>
                                <%}%>                    </div>
                    <div class="tags">
                      <span class="tag">

                        <style>
                          .tag {
                            text-transform: capitalize;
                          }
                        </style>
                        <%= list.location %>, <%=list.specific%>
                      </span>
                    </div>
                    <div class="actions">
                      <a href="/property/<%=list.category%>/<%= list._id %>"><button class="view">View</button></a>
                                <a class="boost" href="/edit/<%=list.category%>/<%=list._id%>"><button>📝 Edit</button></a>

                      <a href="/edit-listing/<%= list._id %>"><button class="edit"
                          onclick="return confirmDelete(event)">Delete</button></a>
                      <a class(<%=list.boosting%>) add s="boost" href="/boost/<%=list.category%>/<%=list._id%>
                            "><button>boost visibility ⚡</button></a>

                    </div>
                  </div>

                </div>
                <% }) %>
                  <% listings.forEach(list=> { %>
                    <div class="card">


                      <img loading="lazy" src="<%= list.bgimg %>" alt="Listing" />
                      <div class="card-content">
                        <h3>
                          <%= list.title %>
                        </h3>
  <%if(list.country=='ghana' ){%>
                            <div class="price">₵<%=Number(list.price).toLocaleString()%>
                            </div>
                            <%}%>
                              <%if(list.country=='nigeria' ){%>
                                <div class="price">₦<%=Number(list.price).toLocaleString()%>
                                </div>
                                <%}%>                        </div>
                        <div class="tags">
                          <span class="tag">

                            <style>
                              .tag {
                                text-transform: capitalize;
                              }
                            </style>
                            <%= list.location %>, <%=list.specific%>
                          </span>
                        </div>
                        <div class="actions">
                          <a href="/property/<%=list.category%>/<%= list._id %>"><button class="view">View</button></a>
                          <a href="/edit-listing/<%= list._id %>"><button class="edit"
                              onclick="return confirmDelete(event)">Delete</button></a>
                                <a class="boost" href="/edit/<%=list.category%>/<%=list._id%>"><button>📝 Edit</button></a>

                          <a class="boost" href="/boost/<%=list.category%>/<%=list._id%>"><button>boost visibility
                              ⚡</button></a>

                        </div>
                      </div>

                    </div>
                    <% }) %>
                      <%vehicle.forEach(list=> { %>
                        <div class="card">


                          <img loading="lazy" src="<%= list.bgimg %>" alt="Listing" />
                          <div class="card-content">
                            <h3>
                              <%= list.title %>
                            </h3>
  <%if(list.country=='ghana' ){%>
                            <div class="price">₵<%=Number(list.price).toLocaleString()%>
                            </div>
                            <%}%>
                              <%if(list.country=='nigeria' ){%>
                                <div class="price">₦<%=Number(list.price).toLocaleString()%>
                                </div>
                                <%}%>                            </div>
                            <div class="tags">
                              <span class="tag">

                                <style>
                                  .tag {
                                    text-transform: capitalize;
                                  }
                                </style>
                                <%= list.location %>, <%=list.specific%>
                              </span>
                            </div>
                            <div class="actions">
                              <a href="/property/<%=list.category%>/<%= list._id %>"><button
                                  class="view">View</button></a>
                              <a href="/edit-listing/<%= list._id %>"><button class="edit"
                                  onclick="return confirmDelete(event)">Delete</button></a>
                              <a class="boost" href="/boost/<%=list.category%>/<%=list._id%>"><button>boost visibility
                                  ⚡</button></a>

                            </div>
                          </div>

                        </div>
                        <% }) %>
                          <% furniture.forEach(list=> { %>
                            <div class="card">


                              <img loading="lazy" src="<%= list.bgimg %>" alt="Listing" />
                              <div class="card-content">
                                <h3>
                                  <%= list.title %>
                                </h3>
  <%if(list.country=='ghana' ){%>
                            <div class="price">₵<%=Number(list.price).toLocaleString()%>
                            </div>
                            <%}%>
                              <%if(list.country=='nigeria' ){%>
                                <div class="price">₦<%=Number(list.price).toLocaleString()%>
                                </div>
                                <%}%>                                </div>
                                <div class="tags">
                                  <span class="tag">

                                    <style>
                                      .tag {
                                        text-transform: capitalize;
                                      }
                                    </style>
                                    <%= list.location %>, <%=list.specific%>
                                  </span>
                                </div>
                                <div class="actions">
                                  <a href="/property/<%=list.category%>/<%= list._id %>"><button
                                      class="view">View</button></a>
                                  <a href="/edit-listing/<%= list._id %>"><button class="edit"
                                      onclick="return confirmDelete(event)">Delete</button></a>
                                <a class="boost" href="/edit/<%=list.category%>/<%=list._id%>"><button>📝 Edit</button></a>

                                  <a class="boost" href="/boost/<%=list.category%>/<%=list._id%>"><button>boost
                                      visibility ⚡</button></a>
                                </div>
                              </div>


                            </div>
                            <% }) %>
    </div>
    <% if (notifications.length>0) { %>
      <div class="notifications-container" id="id">
        <% notifications.forEach(not=> { %>
          <%if(not.isfeedback==false && not.isChat==false){%>
            <a href="/chat/<%=not.sender._id%>">

              <div class="notification-card">
                <button class="close-btn"></button>
                <div class="notification-header"><span>💬</span><strong>New Message</strong></div>
                <p>From <a href="/chat/<%= not.sender._id %>">
                    <%= not.sender.username %>
                  </a> (check inbox)</p>
              </div>
            </a>
            <%} else{%>
              <a href="<%= not.lik%>">

                <div class="notification-card">
                  <button class="close-btn"></button>
                  <div class="notification-header"><span>💬</span><strong>New Message</strong></div>
                  <p>From <a href="<%=not.lik%>">
                      Neonx
                    </a> (check inbox)</p>
                  <p>
                    <%=not.content%>
                  </p>
                  <a href="/chat/feedback/<%=user._id%>">Mark As Read</a>
                </div>
              </a>
              <%}%>
                <% }) %>
      </div>
      <% } %>
  </main>
  <script src="/stop.js"></script>

  <script>
    // Sidebar toggle
    // Sidebar toggle
    const hamburger = document.getElementById("hamburger");
    const sidebar = document.getElementById("sidebar");
    const overlay = document.getElementById("overlay");

    // safe guards in case elements are missing on other pages
    if (hamburger && sidebar && overlay) {
      hamburger.addEventListener("click", () => {
        sidebar.classList.toggle("show");
        overlay.classList.toggle("show");
      });

      // Close sidebar if overlay is clicked
      overlay.addEventListener("click", () => {
        sidebar.classList.remove("show");
        overlay.classList.remove("show");
      });
    }

    // Loader
    window.addEventListener("load", () => {
      setTimeout(() => { document.getElementById("loader").style.opacity = "0"; setTimeout(() => document.getElementById("loader").style.display = "none", 500); }, 1500);
    });
    window.addEventListener("load", () => {
      setTimeout(() => {
        const loaded = document.getElementById("id");
        console.log(loaded)
        console.log(loaded)
        if (loaded) { setTimeout(() => loaded.style.display = 'none'); }
      }, 5000);
    });
    // Confirm logout
    function confirmLogout(e) {
      if (!confirm("Are you sure you want to logout?")) { e.preventDefault(); }
    }
    function confirmDelete(e) {
      if (!confirm("Are you sure you want to delete this ad?")) { e.preventDefault(); }
    }

    // Loader: hide after 1 second
    window.addEventListener("load", () => {
      setTimeout(() => {
        const loader = document.getElementById("loader");
        loader.style.opacity = "0";
        setTimeout(() => loader.style.display = "none", 500);
      }, 1000); // 1 second
    });


  </script>
   <script src="/socket.io/socket.io.js"></script>
                                        <script>
                                            const socket = io();



                                              // Track online users
                                              const onlineUsers = new Set();



                                              // ---- SESSION-BASED AUTH ----
                                              const sessionUserId = socket.request?.session?.userId;

                                              if (sessionUserId) {
                                                socket.userId = sessionUserId.toString();
                                                socket.join(socket.userId);

                                                onlineUsers.add(socket.userId);
                                                io.emit("online-users", Array.from(onlineUsers));

                                                console.log("👤 user joined via session:", socket.userId);
                                              }

                                              // ---- FALLBACK JOIN ----
                                              socket.on("join", ({ userId }) => {
                                                if (!userId) return;

                                                socket.userId = userId.toString();
                                                socket.join(socket.userId);

                                                onlineUsers.add(socket.userId);
                                                io.emit("online-users", Array.from(onlineUsers));

                                                console.log(`👤 socket ${socket.id} joined room ${socket.userId}`);
                                              });


                                              // ---- DISCONNECT ----
                                              socket.on("disconnect", () => {
                                                if (socket.userId) {
                                                  onlineUsers.delete(socket.userId);
                                                  io.emit("online-users", Array.from(onlineUsers));
                                                }

                                                console.log("❌ socket disconnected:", socket.id, socket.userId);
                                              });
                                            
                                        </script>
</body>

</html>
