<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edsam Houses - Choose Listing Plan</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      display: flex;
      background: #f9fafb;
      height: 100vh;
      color: #333;
      overflow: hidden;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    /* Sidebar */
    .sidebar {
      width: 250px;
      background: #fff;
      border-right: 1px solid #eee;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding: 1rem;
      position: fixed;
      top: 0;
      bottom: 0;
      left: 0;
    }

    .logo {
      font-size: 1.5rem;
      font-weight: bold;
      margin-bottom: 2rem;
      color: #2563eb;
    }

    .nav {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .nav a {
      padding: .7rem 1rem;
      border-radius: .5rem;
      font-size: .95rem;
      transition: .3s;
    }

    .nav a:hover,
    .nav a.active {
      background: #2563eb;
      color: #fff;
    }

    .user-box {
      background: #f1f5f9;
      padding: 1rem;
      border-radius: .75rem;
      text-align: center;
    }

    .user-box img {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      margin-bottom: .5rem;
    }

    /* Main */
    .main {
      margin-left: 250px;
      flex: 1;
      overflow-y: auto;
      padding: 2rem;
    }

    header h1 {
      font-size: 2rem;
      color: #111;
      margin-bottom: .8rem;
    }

    header p {
      margin-bottom: 2rem;
      color: #555;
    }

    /* Plans Grid */
    .plans {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
    }

    .card {
      background: #fff;
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0 3px 8px rgba(0, 0, 0, 0.08);
      text-align: center;
      transition: all .3s ease;
      position: relative;
    }

    .card:hover {
      transform: translateY(-6px);
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
    }

    .card h2 {
      font-size: 1.3rem;
      margin-bottom: .5rem;
    }

    .price {
      font-size: 1.6rem;
      font-weight: bold;
      margin: 1rem 0;
    }

    .features {
      list-style: none;
      margin: 1rem 0;
      text-align: left;
      padding-left: 0;
    }

    .features li {
      margin: .4rem 0;
      font-size: .95rem;
    }

    .btn {
      display: inline-block;
      padding: .8rem 1.6rem;
      border-radius: .5rem;
      font-weight: bold;
      font-size: 1rem;
      transition: .3s;
      border: none;
      cursor: pointer;
    }

    /* VIP 4000 (Highlight) */
    .vip-premium {
      background: linear-gradient(135deg, #facc15, #f97316);
      box-shadow: 0 10px 25px rgba(249, 115, 22, 0.4);
      animation: pulseGlow 3s infinite;
      border: 3px solid #f59e0b;
      color: #111;
    }

    .vip-premium h2 {
      font-size: 1.6rem;
      color: #7c2d12;
    }

    .vip-premium .price {
      font-size: 1.8rem;
      color: #b45309;
    }

    .vip-premium .btn {
      background: #2563eb;
      color: #fff;
    }

    .vip-premium .btn:hover {
      background: #1e40af;
      transform: scale(1.1);
    }

    /* VIP 700 */
    .vip-basic {
      border: 2px solid #2563eb;
      background: linear-gradient(135deg, #dbeafe, #93c5fd);
    }

    .vip-basic h2 {
      color: #1e40af;
    }

    .vip-basic .price {
      color: #1e3a8a;
    }

    .vip-basic .btn {
      background: #1e40af;
      color: #fff;
    }

    .vip-basic .btn:hover {
      background: #1d4ed8;
      transform: scale(1.05);
    }

    /* Normal Plan */
    .normal {
      background: #f3f4f6;
      border: 1px solid #ddd;
    }

    .normal h2 {
      color: #16a34a;
    }

    .normal .price {
      color: #15803d;
    }

    .normal .btn {
      background: #16a34a;
      color: #fff;
    }

    .normal .btn:hover {
      background: #15803d;
    }

    /* Animations */
    @keyframes pulseGlow {

      0%,
      100% {
        box-shadow: 0 10px 25px rgba(249, 115, 22, 0.3);
      }

      50% {
        box-shadow: 0 14px 35px rgba(249, 115, 22, 0.6);
      }
    }

    @media (max-width: 768px) {
      .sidebar {
        position: fixed;
        left: -260px;
        top: 0;
        bottom: 0;
        transition: left .3s ease;
        z-index: 1000;
      }

      .sidebar.active {
        left: 0;
      }

      .main {
        margin-left: 0;
        padding: 1rem;
      }

      .menu-btn {
        display: block;
        background: #2563eb;
        color: #fff;
        padding: .5rem 1rem;
        border: none;
        border-radius: .5rem;
        cursor: pointer;
        margin-bottom: 1rem;
      }
    }

    .menu-btn {
      display: none;
    }
  </style>
</head>

<body>
  <!-- Sidebar -->
  <aside class="sidebar">
    <div>
      <div class="logo"><img width="30%" src="/NeonX Logo on Dark Backdrop.png" alt=""></div>
      <nav class="nav">
        <a href="/dashboard">Homepage</a>
        <a href="/profile">Dashboard</a>
        <a href="/sell" class="active">Add Listing</a>
        <a href="/inbox">Messages</a>
        <a href="/user">Profile</a>
        <a href="/freinds">My Friends</a>
        <a href="/saved">Saved Ads</a>
        <a href="/logout" onclick="return confirmLogout(event)">Logout</a>
      </nav>
    </div>
    <style>
      .profile-avatar {
        width: 56px;
        height: 56px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.4rem;
        font-weight: 600;
        color: #fff;
        margin: 0 auto 0.5rem;
        user-select: none;
      }
    </style>
    <div class="user-box">
      <div class="profile-avatar" data-name="<%= user.username %>">
        <%= user.username.charAt(0).toUpperCase() %>
      </div>
      <strong>
        <%= user.username %>
      </strong>
      <small>Premium Seller</small>
    </div>
    <script>
      function getAvatarColor(name) {
        const colors = [
          "#1a73e8", // Google blue
          "#34a853", // Google green
          "#fbbc05", // Google yellow
          "#ea4335", // Google red
          "#9334e6", // purple
          "#f06292", // pink
          "#00acc1", // cyan
          "#5c6bc0"  // indigo
        ];

        let hash = 0;
        for (let i = 0; i < name.length; i++) {
          hash = name.charCodeAt(i) + ((hash << 5) - hash);
        }

        return colors[Math.abs(hash) % colors.length];
      }

      document.querySelectorAll(".profile-avatar").forEach(el => {
        const name = el.dataset.name || "U";
        el.style.backgroundColor = getAvatarColor(name);
      });
    </script>
  </aside>

  <!-- Main -->
  <main class="main">
    <button class="menu-btn">☰ Menu</button>
    <header>
      <h1>Choose Your Listing Plan</h1>
      <p>Select the best way to showcase your property on <strong>Neonx</strong>. <a class="back" href="/profile">Back
          to Homepage</a></p>
    </header>

    <div class="plans">
      <!-- Normal Plan -->
      <div class="card normal">
        <h2>🏠 Normal Listing</h2>
        <div class="price">Free</div>
        <ul class="features">
          <li>✔ Standard property listing</li>
          <li>✔ Visible in searches</li>
          <li>✔ Per property listing</li>
        </ul>
        <a href="/free" class="btn">Choose Normal</a>
      </div>
      <!-- VIP Premium Plan -->
      <div class="card vip-premium">
        <h2>🔥 VIP Subscription (Best Value)</h2>
        <%if(user.country=='ghana' ){%>
          <div class="price">₵40
          </div>
          <%}%>
            <%if(user.country=='nigeria' ){%>
              <div class="price">₦4,000
              </div>
              <%}%>
                <ul class="features">
                  <li>✔ Get <b>5 VIP Credits</b></li>
                  <li>✔ Highlighted & promoted listings</li>
                  <li>✔ Always shown first to buyers</li>
                  <li>✔ Recommended in searches</li>
                </ul>
                <form action="/vips/pays" method="POST">
                  <button type="submit" class="btn">Buy Now <%if(user.country=='ghana' ){%>
                      ₵40

                      <%}%>
                        <%if(user.country=='nigeria' ){%>
                          ₦4,000

                          <%}%></button>
                </form>
                <p style="margin-top:15px;">You currently have <b>
                    <%= user.credit %>
                  </b> VIP credits.</p>
      </div>

      <!-- VIP Basic Plan -->
      <div class="card vip-basic">
        <h2>⭐ VIP Listing</h2>
        <%if(user.country=='ghana' ){%>
          <div class="price">₵7
          </div>
          <%}%>
            <%if(user.country=='nigeria' ){%>
              <div class="price">₦700
              </div>
              <%}%>
                <ul class="features">
                  <li>✔ Highlighted listing</li>
                  <li>✔ Shown before normal posts</li>
                  <li>✔ Recommended in search results</li>
                </ul>
                <a href="/pay/vip" class="btn">
                  <%if(user.country=='ghana' ){%>
                    Choose ₵7
                    <%}%>
                      <%if(user.country=='nigeria' ){%>
                        ₦700

                        <%}%>
                </a>
      </div>


    </div>
  </main>

  <script>
    const sidebar = document.querySelector('.sidebar');
    const menuBtn = document.querySelector('.menu-btn');
    menuBtn.addEventListener('click', () => {
      sidebar.classList.toggle('active');
    });

    function confirmLogout(event) {
      if (!confirm("Are you sure you want to logout?")) {
        event.preventDefault();
        return false;
      }
      return true;
    }
  </script>
</body>

</html>